from django.apps import AppConfig


class SalesChainConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sales_chain'
    verbose_name = 'сети по продаже электроники'
